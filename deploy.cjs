const { spawn } = require('child_process'); const surge = spawn('npx.cmd', ['surge', './dist', 'app-latinbarbers.surge.sh'], { shell: true }); surge.stdout.on('data', (data) => { const output = data.toString(); console.log('STDOUT:', output); if (output.toLowerCase().includes('email')) { surge.stdin.write('thelatinbarbersxyz@example.com\n'); } else if (output.toLowerCase().includes('password')) { surge.stdin.write('SenhaBarbearia123!\n'); } }); surge.stderr.on('data', (data) => console.error('STDERR:', data.toString())); surge.on('close', (code) => console.log('Exited', code));
